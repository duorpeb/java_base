package day08;

import java.util.Scanner;

public class Class02 {
	public static void main(String[] args) {
		
	}
}


class Car{
	// 멤버 변수 - name, color, year, power, speed
private
	String name, color, year;
	boolean power;
	int speed;

	// 메서드
	// 메서드 1) display() - 현재 상태 (멤버 변수 상태) 출력하는 메소드 생성
	// 메서드 2) start() - 시동을 켜고 끄는 메소드 생성 
	// 메서드 3) speed() - 속도를 증감시킬 수 있는 메소드 생성 
	// 메서드 4) Getter, Setter 메소드 
public
	// 메서드 1) display
	void display() {
		System.out.println("<자동차 정보>");
		System.out.print("자동차 이름 : " + getName());
		System.out.print("자동차 색상 : " + getColor());
		System.out.print("자동차 연식 : " + getYear() + "년식");
		
		System.out.println("(On / Off) : " + getStart());
		System.out.println("속도 : " + getSpeed());
	}

	
	// 메서드 2) start()
	boolean start() {
		power = false;
		String rsp;
		
		System.out.println("시동을 거시겠습니까? (Y/N)");
		Scanner sc = new Scanner(System.in);
		
		rsp = sc.next().toUpperCase();
		
		if(rsp.charAt(0) == 'Y') { power = true; }
			else { power = false; }
		
		sc.close();
		
		return power;
	}
	
	
	// 메서드 3) speed()
	int speed() {
		char carSpeed;
		int speedNum = 10;
		
		System.out.println("Speed Up (+) / Speed Down (-)");
		Scanner sc = new Scanner(System.in);
		
		carSpeed = sc.next();
		
		switch(carSpeed) {
			case '+' : setSpeed(getSpeed(sp));
			case '-' : return (speedNum - 10);
		}
		
		return speedNum;
	}
	
	
	// 메서드 4) Getter
	String getName() { return name; }
	
	String getColor() { return color; }
	
	String getYear() { return year; }
	
	boolean getStart() { return power; }
	
	int getSpeed() { return speed; }

	
	
	
	// 메서드 4.1) Setter
	// setName()
	void setName(String name) { 
		name = "Sonata";
	}
	
	
	// setColor()
	void setColor(String color) { 
		color = "White";
	}
	
	
	// setYear()
	void setYear(String year) { 
		year = "2018";
	}
	
	
	// setStart()
	void setStart(boolean bool) { this.power = bool; }

	// setSpeed()
	void setSpeed(int x) { this.speed = x; }
	
}