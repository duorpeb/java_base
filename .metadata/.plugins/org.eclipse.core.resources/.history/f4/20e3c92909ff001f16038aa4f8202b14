// static 이 사용된 멤버 변수 혹은 메서드는 클래스의 멤버 변수 / 메서드라고 함 
// static 은 인스턴스 생성 (객체 생성) 없이 
// 클래스가 만들어지면 생성되기에 인스턴스 생성 없이 클래스만으로 호출 가능 
// 하나의 멤버 변수가 모든 객체에서 사용해야 할 때 static 키워드 사용

// static 이 안붙은 멤버 변수, 메서드는 인스턴스 (객체) 의 멤버변수 / 메서드 라고 함 
// 즉, static 이 붙지 않은 멤버 변수 / 메서드는 인스턴스 생성 없이 사용할 수 없음 
// 각각의 멤버 변수 / 메서드는 독립적인 멤버 변수 / 메서드를 의미 

// 클래스와 객체는 생성되는 시점이 달라서 메서드와 멤버 변수를 사용할 수 있는 환경이 다름 
package day10;

public class Static01 {
	
}
